# Downloaded team code and indexes are large and reproducible — never commit them.
data/repos/
data/index/
data/cache/
# Year-grouped corpus built by the scout/ program (symlinked to local disk).
frc_team_repos/
frc_team_repos
# DuckDB code index (derived, rebuildable from the corpus).
data/*.duckdb
data/*.duckdb.wal
# uv venv for the index extras.
.venv/
*.wpilog
*.hoot
*.rlog
.venv/
node_modules/
__pycache__/
*.pyc
.DS_Store
# Jekyll book site — generated from knowledge/ by scripts/build_site.py; build artifacts.
docs/book/
docs/_site/
docs/.jekyll-cache/
docs/.jekyll-metadata
docs/.bundle/
docs/vendor/
docs/Gemfile.lock
