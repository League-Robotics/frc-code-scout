[project]
name = "frc-code-scout"
version = "0.1.0"
description = "Build a multi-year FRC team code corpus (commit-history + suppressed source) and a Statbotics EPA dataset"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
